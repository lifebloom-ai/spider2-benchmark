SELECT CLASS, SUBCLASS, SUM(LENGTH_M) AS total_length
FROM NETHERLANDS_OPEN_MAP_DATA.NETHERLANDS.V_ROAD
WHERE QUADKEY LIKE '1202021%'
AND LENGTH_M IS NOT NULL
GROUP BY CLASS, SUBCLASS
ORDER BY total_length DESC;